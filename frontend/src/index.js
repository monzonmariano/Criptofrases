import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.jsx'; // Ruta actualizada para la importación

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
